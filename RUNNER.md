# L'agent de calcul

**Dernier passage : aucun pour l'instant.**

**Demandes en attente dans `jobs/` : 0.**

Ce fichier est le témoin de vie de l'agent. Il se réécrit tout seul à chaque passage.
L'agent regarde dans `jobs/` toutes les dix minutes.

> **Si la date ci-dessus a plus de deux heures alors que le PC est allumé,
> l'agent ne tourne plus.** Recoller la ligne 3 de [`COMMANDE.md`](COMMANDE.md).

| Passage | Demande | Résultat |
|---|---|---|
| — | — | la tâche n'a pas encore été créée sur le PC |

Journaux détaillés des passages : `~/.runner-journaux/` sur le PC.
